These files are archived UI explorations kept for reference.

The active runtime template is `bindery/templates/index.html`.

Archived here:
- early concept directions (`v1`-`v12`)
- narrowed finals used during design exploration

They are not part of the served Bindery web stack.
